
<div class="leftNews">
  <h1><a href="/homepage/news_centre">更多&gt;&gt;</a>活动咨询</h1>
  <ul>
    <% (@sales_all || []).each do |sale| %>
      <li><span><a href="#"><%=sale.name[0,10] %></a></span><%=show_time(sale.created_at) %></li>
    <%end%>
  </ul>
</div>
<div class="leftInquire">
  <h1>门店查询</h1>
  <div class="inquire_side"><label>选择省市：</label>
      <select id="city_province_id"><option value="0">--请选择--</option>
        <% (province || []).each do |p|%>
          <option value=<%= p.id %>><%= p.name %></option>
        <%end%>
      </select>
    
    <select id="city_city_id"></select>
  </div>
  <div class="clear"></div>
  <div class="inquire_side">
    <ul id="stores_list">
    </ul>
  </div>
</div>
<%= javascript_include_tag "left" %>